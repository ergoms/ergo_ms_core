---
alwaysApply: false
---
# КРИТИЧЕСКИ ВАЖНО: Правила клиентского кода

Всю локализацию интерфейса всегда делай на русском языке, и только по отдельной просьбе пиши на другом языке.

Разрабатывай всегда интерфейс так, чтобы он был адаптивным, и подходил под мобильные устройства.

При реализации vue.js компонентов старайся декомпозировать компоненты и использовать стили bootstrap, а также существующую стилистику клиентского решения. Выноси CSS/SCSS и .js логику в отдельные файлы только когда компонент превышает 1000 строк.

Все иконки в client бери из библиотеки lucide, и делай их всегда по центру относительно текста, ты часто делаешь их чуть ниже текста. 

С всплывающими уведомлениями работай вот так всегда:
import { useToast } from 'vue-toastification'
const toast = useToast()

Всегда используй модальные окна (client/src/components/ConfirmDialog.vue) вместо алертов (alert) для подтверждения каких то операций.

# Работа с импортами
Если ты используешь зависимость из ядра системы (core/client) то пиши пути используя абсолютные пути:

import { apiClient } from '@/js/api/manager'
import { endpoints } from '@/js/api/endpoints'

Даты, время и склонение относительного времени на русском — только через `@/js/utils/timeUtils.js` (подробности в `time-utils.mdc`).

Если ты используешь внутримодульные зависимости (modules/<имя_модуля>), то пиши пути используя относительные импорты.


# defineProps и defineEmits
В vue.js не делай данные импорты, потому что - [@vue/compiler-sfc] `defineProps` is a compiler macro and no longer needs to be imported.