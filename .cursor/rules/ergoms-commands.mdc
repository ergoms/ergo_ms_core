---
description: Техническая справка ergoms — commands.conf, help.manifest.yaml, help modules/module, префиксы api/npm
alwaysApply: false
---

# Команды ergoms (для разработчиков)

Поведение для пользователя и ассистента — [`no-direct-manage-py.mdc`](no-direct-manage-py.mdc). Справочник команд — [`.docs/cli.md`](../../.docs/cli.md). Устройство развёртывания — [`core/deployment/logic.md`](../../core/deployment/logic.md). Обзор pipeline для людей — [`.docs/lifecycle-pipeline.md`](../../.docs/lifecycle-pipeline.md).

## Справка (три режима)

| Команда | Источник |
|---------|----------|
| `ergoms help` | [`core/deployment/help.manifest.yaml`](../../core/deployment/help.manifest.yaml) + [`ergoms_help.py`](../../core/deployment/scripts/ergoms_help.py) |
| `ergoms help modules` | `modules/*/ergoms.help.yaml` + scan `ergoms.conf` |
| `ergoms help module <имя>` | `modules/<имя>/ergoms.help.yaml` |

Текст help — **русский в источнике** (`help.manifest.yaml`, `ergoms.help.yaml`); переводы — overlay в `core/deployment/locales/<lang>/help.manifest.yaml` и `modules/<имя>/locales/<lang>/ergoms.help.yaml` (deep-merge в runtime). Модульная справка — см. [`ergoms-module-help.mdc`](ergoms-module-help.mdc).

## Язык консольного вывода

Пользовательский вывод ergoms **ядра** — через каталоги `core/deployment/locales/<lang>/cli_messages.yaml`, не литералы в скриптах:

- shell `core/deployment/windows/`, `core/deployment/linux/`
- Python `core/deployment/scripts/` (кроме docstring и argparse для CI)
- `Write-ErgomsMessage -Key`, `write_ergoms_message --key`, `t('key')` в Python

**Язык CLI:** `ERGO_CLI_LANGUAGE` → системная локаль → `ru` ([`cli_locale.py`](../../core/deployment/cli_locale.py)). `DEFAULT_LANGUAGE` для CLI не используется.

**Допустимый английский:** имена команд (`ergoms setup`), путей, служб (`ergo_ms_api_dev`), флагов CLI, устоявшихся имён инструментов (nginx, Redis, Poetry, Celery); **метки** `[OK]`, `[ERROR]`, `[WARNING]`, `[SKIP]`, `[INFO]` — см. [`console-output.mdc`](console-output.mdc).

**Обязательно:**

- новые строки — ключ в `locales/ru/cli_messages.yaml` + overlay для `en`/`fr` (и др. языков с каталогом `locales/<code>/`);
- повторяющиеся шаблоны — `ergoms_console.py` / `Write-ErgomsMessage` с ключами, не дублировать текст;
- Windows `.ps1` с кириллицей в строках — **UTF-8 BOM** (PowerShell 5.1)

**Запрещено:** хардкод пользовательских фраз (`Failed to`, `Usage:`, `Unknown service`) вместо ключей `cli_messages`; вложенные карты `{ru: …, en: …}` в одном YAML.

При добавлении команды ядра — ключи в `cli_messages` и `summary` (русский) в `help.manifest.yaml` + overlay при необходимости.

## Единый lifecycle-pipeline

Составные сценарии (setup, deploy, службы, nginx/redis/TLS, Docker compose, dev) — через [`lifecycle/runner.py`](../../core/deployment/lifecycle/runner.py) → [`DeploymentOrchestrator.run_recipe`](../../core/deployment/lifecycle/orchestrator.py). Реестр рецептов — [`lifecycle/recipes.py`](../../core/deployment/lifecycle/recipes.py). Полное правило — [`lifecycle-pipeline.mdc`](lifecycle-pipeline.mdc).

| Точка входа | Роль |
|-------------|------|
| `commands.conf` | `runner.py <recipe>` для составных команд |
| `ergo_ms.ps1` / `ergo_ms.sh` | Маршрутизация + тонкое делегирование в runner |
| `docker_cli.py` | argparse → `run_recipe('docker-*')` |
| `.vscode/tasks.json` | Только `ergoms <команда>`, без прямого `ergo_ms.ps1` / `sudo bash ergo_ms.sh` |

Список рецептов: `python core/deployment/lifecycle/runner.py --list`.

Низкоуровневые примитивы (`api:`, `npm:`, прямой `start_*.py`) остаются в `commands.conf` для разработки.

## Где описаны команды

- Ядро: `core/deployment/commands.conf` + описания в `help.manifest.yaml`
- Модули: `modules/<имя>/ergoms.conf` → вызов `ergoms <имя>:<команда>`; описания — `ergoms.help.yaml`

Формат строки: `имя-команды=тип:команда`. Несколько шагов: `&&`.

## Команды вне commands.conf

**install-cli**, **help**, маршрутизация — в `ergo_ms.ps1` / `ergo_ms.sh`. **nginx**, **redis**, **TLS**, службы (`install-services`, `start`, `stop`, …) и **setup-full** делегируют в [`lifecycle/runner.py`](../../core/deployment/lifecycle/runner.py) (рецепты `nginx-*`, `redis-*`, `tls-*`, `service-*`, `setup-full`). На Linux `sudo` — внутри runner ([`privilege.py`](../../core/deployment/lifecycle/host/privilege.py)), не в VS Code tasks.

Описания для `ergoms help` — в `help.manifest.yaml`. Подробности — [`deployment-infra.mdc`](deployment-infra.mdc), [`core/deployment/logic.md`](../../core/deployment/logic.md).

**Docker Compose** — в `commands.conf` (префикс `docker-*`); CLI [`docker_cli.py`](../../core/deployment/docker/docker_cli.py) делегирует `up`/`down`/`build`/`ps`/`logs` в рецепты compose. Правила — [`docker.mdc`](docker.mdc).

При добавлении **составной** команды — рецепт в `recipes.py`, строка в `commands.conf` → `runner.py`, `summary` в help. Для **новой** infra-команды — шаг в `infra_steps.py` или `internal_dispatch`, рецепт, делегирование в `ergo_ms.ps1` / `ergo_ms.sh`, `_BUILTIN_COMMANDS` в [`ergoms_help.py`](../../core/deployment/scripts/ergoms_help.py).

## Точки входа Django-команд ядра

Помимо management-команд Django (discovery, вызов через `ergoms api <команда>`), в `core/api/` есть два отдельных механизма:

- `core/api/commands/` — реализация команд `ergoms api install`, `module-add`, `module-remove`, `module-list` (управление зависимостями модулей в `modules/<имя>/pyproject.toml`, точка входа — `commands/__main__.py`).
- `core/api/scripts/` — точки входа процессов (`start_api.py`, `start_celery_worker.py`, `start_celery_beat.py`, `start_media_api.py`, `start_jupyter.py`, `warmup_caches_if_needed.py`), которые `commands.conf` вызывает напрямую через `virtual_env/python/.../python.exe core/api/scripts/<файл>.py` (без прокси `api:`). **Те же скрипты** — `command` сервисов в Docker Compose; при правке запуска обновляй и compose — см. [`docker.mdc`](docker.mdc).

## Типы префиксов

| Префикс | Где выполняется |
|---------|-----------------|
| `api:` | `core/api/`, команды Django |
| `poetry:` | корень проекта |
| `npm:` | `virtual_env/npm/` (клиентский workspace) |
| `shell:` | корень, любая ОС |
| `win:` / `linux:` | только своя ОС |

## Прокси

- `ergoms api migrate` — команда Django напрямую
- `ergoms poetry install`
- `ergoms npm run build`

Аргументы после имени команды передаются автоматически.

## Примеры в commands.conf

```conf
setup=win:py -3.12 core\deployment\lifecycle\runner.py setup-full && linux:python3.12 core/deployment/lifecycle/runner.py setup-full
install-deps=win:virtual_env\python\Scripts\python.exe core\deployment\lifecycle\runner.py install-deps && linux:...
start-all-services=win:... runner.py start-all && linux:...
dev=win:... runner.py dev-api && linux:...
migrate-all=api:makemigrations && api:migrate
```

Составные цепочки `api:install && npm:...` для deploy/setup **заменены** на один рецепт runner. Примитивы `api:migrate`, `npm:run build` — по-прежнему допустимы.

Имена команд — kebab-case (`db-migrate`, не `db_migrate` при вызове через ergoms).

## Новая команда ядра

1. Для **составной** команды — рецепт в [`lifecycle/recipes.py`](../../core/deployment/lifecycle/recipes.py) и строка `runner.py <recipe>` в `commands.conf` (или builtin → runner в `ergo_ms.ps1` / `ergo_ms.sh`).
2. Для **примитива** — строка `api:` / `npm:` / `shell:` в `commands.conf`.
3. Добавь `summary` в `core/deployment/help.manifest.yaml`.
4. При необходимости — шаг в `lifecycle/steps/` или скрипт в `core/deployment/scripts/`.
5. Проверь на обеих ОС ([`oc.mdc`](oc.mdc)).

Модульную команду — в `modules/<имя>/ergoms.conf` и описание в `ergoms.help.yaml`.

## Если команда не работает

1. `ergoms help` / `ergoms help modules` — есть ли команда
2. Проверь `commands.conf` / `ergoms.conf`
3. Для отладки: `ergoms api <команда_django> <аргументы>`

## Связанные правила

| Тема | Файл |
|------|------|
| Lifecycle-pipeline (детали) | [`lifecycle-pipeline.mdc`](lifecycle-pipeline.mdc) |
| UTF-8 BOM в .ps1 | [`ps1-encoding.mdc`](ps1-encoding.mdc) |
| Справка модулей | [`ergoms-module-help.mdc`](ergoms-module-help.mdc) |
| Только ergoms, не manage.py | [`no-direct-manage-py.mdc`](no-direct-manage-py.mdc) |
| Python-окружение | [`virtual-env.mdc`](virtual-env.mdc) |
| npm workspace | [`npm-workspace.mdc`](npm-workspace.mdc) |
| Windows / Linux | [`oc.mdc`](oc.mdc) |
| Redis, nginx, TLS | [`deployment-infra.mdc`](deployment-infra.mdc) |
