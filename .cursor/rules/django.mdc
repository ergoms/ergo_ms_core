---
alwaysApply: false
---
# Django (форк проекта)

Django лежит в submodule `core/django` — модели могут отличаться от стандартных.

В `AbstractUser` добавлено поле `middle_name`. Перед правками auth смотри форк.

Админ-панель Django **отключена** — не создавай `admin.py` и admin-регистрации.
