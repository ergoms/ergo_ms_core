---
description: Справка ergoms для модулей — ergoms.help.yaml рядом с ergoms.conf, help module, русский текст
globs: "modules/**/ergoms.conf"
alwaysApply: false
---

# Справка ergoms в модулях

Команды модуля описываются в **`modules/<имя>/ergoms.conf`**, справка для пользователя — в **`modules/<имя>/ergoms.help.yaml`**. Ядро не хранит описания модульных команд.

## Кратко

- Вызов: `ergoms <имя_модуля>:<команда> [аргументы]`
- Оглавление модулей: `ergoms help modules`
- Справка по модулю: `ergoms help module <имя>`
- Текст справки — **только русский**; имена команд и путей — как в коде

## Обязательно

При добавлении или изменении строки в `ergoms.conf`:

1. Создай или обнови **`ergoms.help.yaml`** в том же каталоге модуля.
2. Для каждой команды conf добавь `commands.<имя>.summary` на русском.
3. Поле `module` в yaml совпадает с именем каталога (`kebab-case`).
4. Эталон — [`modules/module_template/ergoms.help.yaml`](../../modules/module_template/ergoms.help.yaml).

## Формат ergoms.help.yaml

```yaml
module: video_analysis
title: "Анализ видео"
summary: "Кратко о назначении модуля"

commands:
  install:
    summary: "Установить все зависимости модуля"

notes:
  - "Вызов: ergoms video_analysis:<команда>"
```

| Поле | Назначение |
|------|------------|
| `module` | Имя каталога модуля |
| `title` | Заголовок в `ergoms help module` |
| `summary` | Одна строка для `ergoms help modules` |
| `commands.<имя>.summary` | Описание команды (ключ = имя в ergoms.conf без префикса) |
| `notes` | Дополнительные примечания (необязательно) |

## Запрещено

- Описания модульных ergoms-команд в `core/deployment/help.manifest.yaml` или в `ergoms_help.py`
- Английский текст в `ergoms.help.yaml`
- Команда в conf без `summary` в help (при `help module` будет предупреждение)

## Где смотреть

| Что | Файл |
|-----|------|
| Рендер справки | [`core/deployment/scripts/ergoms_help.py`](../../core/deployment/scripts/ergoms_help.py) |
| Справка ядра | [`core/deployment/help.manifest.yaml`](../../core/deployment/help.manifest.yaml) |
| Эталон модуля | [`modules/module_template/ergoms.help.yaml`](../../modules/module_template/ergoms.help.yaml) |

## Связанные правила

| Тема | Файл |
|------|------|
| Структура модулей | [`modules.mdc`](modules.mdc) |
| Команды ergoms | [`ergoms-commands.mdc`](ergoms-commands.mdc) |
| Стиль текста | [`writing-docs-and-rules.mdc`](writing-docs-and-rules.mdc) |
