---
description: Celery — ergoms start-worker/beat, задачи в modules/, celery_config, логи
alwaysApply: false
---
# Фоновые задачи (Celery)

## Запуск

- Worker: `ergoms start-worker` (или `--worker=<имя>`)
- Планировщик: `ergoms start-beat`
- Остановка: `ergoms stop-worker`, `ergoms stop-beat`

Worker и Beat должны смотреть на **один** брокер (настройки в `databases.yaml`: секции `celery`, `celery_worker`, `celery_beat`).

## Где писать код

**В модуле**, не в ядре (в ядре только инфраструктура, например отправка писем):

- `modules/<имя>/api/tasks.py` — задачи с `@shared_task`
- `modules/<имя>/api/celery_config.py` — очереди и маршруты
- `modules/<имя>/api/celery_beat_config.py` — расписание (если нужно)

Пример маршрута:

```python
'modules.my_module.api.tasks.*': {'queue': 'my_module'}
```

Регистрация вручную не нужна — подхватывает `CeleryModuleManager`.

## Логи

См. [`logging.mdc`](logging.mdc) — единая схема, формат, env, `ergoms logs`.

Кратко: задачи модулей → `celery_tasks.log` (`celery.module.<имя>`); Beat модулей → `celery_beat.log` (`celery.beat.module.<имя>`). Отдельные `logs/modules/` и `logs/celery/beat/` **не используются**.

```powershell
ergoms logs celery-tasks bi_analysis 200
ergoms logs celery-beat notifications 200
```

## После смены расписания

Перезапусти Beat — он синхронизирует задачи с БД.

## Связанные правила

| Тема | Файл |
|------|------|
| Команды ergoms | [`no-direct-manage-py.mdc`](no-direct-manage-py.mdc) |
| Логи (файлы, env, Celery) | [`logging.mdc`](logging.mdc) |
| Модули, tasks.py | [`modules.mdc`](modules.mdc) |
| Брокер в databases.yaml | [`.docs/configuration.md`](../../.docs/configuration.md) |

## Не делай

- Не правь `core/api/src/config/celery.py` без крайней нужды
- Не ставь доменные задачи в ядро
- Не предлагай перезапуск worker/beat/API, если пользователь не просил
