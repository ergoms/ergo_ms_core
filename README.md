# ERGO MS

Настоящий документ формализует принципы проектирования, развертывания и эксплуатации интеллектуального фреймворка ERGO MS. Система ориентирована на построение и оркестрацию модульных решений любой предметной области, обеспечивая строгую изоляцию ядра и подключаемых доменных компонентов.

## Описание

ERGO MS представляет собой интеллектуальный фреймворк для проектирования и оркестрации модульных программных систем, реализованный на основе архитектурного паттерна «ядро + модули». Фреймворк обеспечивает строгую изоляцию компонентов, унифицированные интерфейсы взаимодействия и декларативную конфигурацию развёртывания.

**Технологический стек:**
- Серверная часть: Django 5.2, Django REST Framework
- Клиентская часть: Vue.js 3, Vite
- Система управления базами данных: PostgreSQL 14+ | SQLite | MySQL
- Обработка асинхронных задач: Celery, Redis
- Управление зависимостями: Poetry (Python), npm (Node.js)

## Содержание

### 1. Архитектура и принципы проектирования
- [1.1. Особенности работы с фреймворком ERGOMS](#особенности-работы-с-фреймворком-ergoms)
  - [1.1.1. Модульная архитектура и Git submodules](#модульная-архитектура-и-git-submodules)
  - [1.1.2. Модульные переменные окружения](#модульные-переменные-окружения)
  - [1.1.3. Структура модулей](#структура-модулей)
  - [1.1.4. Модели и миграции](#модели-и-миграции)
  - [1.1.5. Задачи Celery](#задачи-celery)
  - [1.1.6. Клиентская архитектура](#клиентская-архитектура)
  - [1.1.7. Интеграция с Cursor](#интеграция-с-cursor)
  - [1.1.8. Правила разработки Cursor (Rules)](#правила-разработки-cursor-rules)
  - [1.1.9. Средства разработки](#средства-разработки)
  - [1.1.10. Платформенная совместимость](#платформенная-совместимость)
  - [1.1.11. Управление окружением](#управление-окружением)
  - [1.1.12. Модульность библиотек](#модульность-библиотек)
  - [1.1.13. Командная оболочка ergoms](#командная-оболочка-ergoms)
  - [1.1.14. Django-автоматизация в рамках ERGOMS](#django-автоматизация-в-рамках-ergoms)
  - [1.1.15. Vue.js-автоматизация в рамках ERGOMS](#vuejs-автоматизация-в-рамках-ergoms)
  - [1.1.16. Интеграция Bootstrap и стилевая архитектура](#интеграция-bootstrap-и-стилевая-архитектура)
  - [1.1.17. Агрегация переиспользуемых компонентов](#агрегация-переиспользуемых-компонентов)

### 2. Структура проекта
- [2.1. Конфигурационные файлы](#конфигурационные-файлы)

### 3. Развёртывание и инициализация
- [3.1. Быстрый старт](#быстрый-старт)
  - [3.1.0. КРИТИЧНОЕ ТРЕБОВАНИЕ: Путь к системе](#быстрый-старт)
  - [3.1.1. Этап 1: Требуемые программные зависимости](#быстрый-старт)
  - [3.1.2. Этап 2: Клонирование репозитория](#быстрый-старт)
  - [3.1.3. Этап 3: Инициализация конфигурационных файлов](#быстрый-старт)
  - [3.1.4. Этап 4: Установка зависимостей и инициализация окружения](#быстрый-старт)
  - [3.1.5. Этап 5: Запуск системы в режиме разработки](#быстрый-старт)
  - [3.1.6. Этап 6: Сетевые точки доступа](#быстрый-старт)
- [3.2. Настройка конфигурационных файлов](#настройка-конфигурационных-файлов)
  - [3.2.1. Настройка .env](#настройка-env)
  - [3.2.2. Настройка databases.yaml](#настройка-databasesyaml)
  - [3.2.3. Настройка menu-order-config.json](#настройка-menu-order-configjson)
  - [3.2.4. Возможность совместной работы (Live Share)](#возможность-использования-совместной-работы-на-одном-компьютере)

### 4. Управление системой
- [4.1. Запуск системы разработки](#запуск-системы-разработки)
  - [4.1.1. Хранение логов](#хранение-логов)
- [4.2. Управление сервисами через CLI](#управление-сервисами-через-cli)
  - [4.2.1. Базовые операции](#базовые-операции)
  - [4.2.2. Компонентный запуск](#компонентный-запуск)
  - [4.2.3. Наблюдение за системой](#наблюдение-за-системой)
  - [4.2.4. Миграции и статика](#миграции-и-статика)
  - [4.2.5. Управление зависимостями](#управление-зависимостями)
  - [4.2.6. Сборка проекта](#сборка-проекта)
- [4.3. Дополнительные команды CLI](#дополнительные-команды-cli)
  - [4.3.1. Установка и управление службами](#установка-и-управление-службами)
  - [4.3.2. Управление CLI-обёрткой](#управление-cli-обёрткой)
  - [4.3.3. Комплексные сценарии развёртывания](#комплексные-сценарии-развёртывания)
- [4.4. Прокси-команды](#прокси-команды)
- [4.5. Модульные команды](#модульные-команды)
- [4.6. Практические сценарии](#практические-сценарии)
- [4.7. Конфигурация CLI](#конфигурация-cli)

### 5. Системная интеграция
- [5.1. Разворачивание системных служб (Linux)](#разворачивание-системных-служб-linux--скрипт-linuxergo_mssh)

### 6. Диагностика и устранение неисправностей
- [6.1. Решение проблем при установке](#решение-проблем-при-установке)
  - [6.1.1. Проблема 1: Permission denied при установке Poetry пакетов](#проблема-1-permission-denied-при-установке-poetry-пакетов)
  - [6.1.2. Проблема 2: Команда ergoms не найдена](#проблема-2-команда-ergoms-не-найдена)
  - [6.1.3. Проблема 3: База данных не настроена](#проблема-3-база-данных-не-настроена)

---

## Особенности работы с фреймворком ERGOMS

### Модульная архитектура и Git submodules
- Архитектура системы задаётся манифестом `.gitmodules`: ядро и каждый доменный модуль подключаются посредством механизма Git submodule, обеспечивая независимую разработку компонентов и выборочное обновление через `git submodule update`.
- Ядро образуется субмодулями `core/api`, `core/client`, `core/media_api`, а также форками `core/django` и `core/django_rest_framework`. В последних удалён функционал стандартной административной панели Django ввиду отсутствия необходимости её использования.
- Каждая директория `modules/<имя>` представляет собой обособленный submodule (например, `modules/video_analysis`, `modules/crm`). Модули взаимодействуют с ядром через унифицированный API-контракт и клиентский интерфейс, сохраняя изоляцию, что обеспечивает упрощённую поддержку и масштабируемость.

### Модульные переменные окружения
- Каждый модуль располагает собственным файлом `.env`, размещённым в корневой директории модуля совместно с каталогами `api` и `client`. Фреймворк осуществляет адаптивное объединение модульных переменных окружения с глобальными настройками, вследствие чего требуется описание исключительно отличий от базовой конфигурации.

### Структура модулей
- Базовая структура модуля образуется парой каталогов `api` и `client`. В файле `api/apps.py` реализуется класс `AppConfig`, осуществляющий регистрацию модуля, конфигурацию путей к пакетам, переключение режимов GPU/CPU и подключение сигналов. Используются стандартизированные пути, определённые в `core/api/src/config/settings/base.py`.
- Для фоновых задач формируются файлы `celery_config.py` и `celery_beat_config.py`, расширяющие класс `CeleryModuleConfig`. В них декларируются маршруты задач, очереди обработки и расписания выполнения без внесения изменений в ядро системы.

### Модели и миграции
- Определение таблиц и связей осуществляется в файле `models.py`. Миграции генерируются стандартными командами Django либо ERGO CLI, при этом проектирование моделей производится с соблюдением нормальных форм реляционных баз данных и учётом зависимостей между модулями.

### Задачи Celery
- Файл `tasks.py` импортируется системой Celery автоматически при инициализации воркеров. В нём располагаются пайплайны обработки данных, включая высоконагруженные задачи, масштабируемые посредством Celery с учётом параметров GPU/CPU, определённых в `apps.py`.

### Клиентская архитектура
- Фронтенд-файлы `client/js/endpoints.js`, `client/js/routes.js` и `client/js/menu-config.json` обнаруживаются фреймворком автоматически: они формируют точки входа API, регистрируют маршруты клиентского приложения и определяют элементы навигационного меню без необходимости модификации ядра.
- Vue-компоненты подлежат декомпозиции: бизнес-логика выделяется в файлы `.js` либо `.ts`, стилевое оформление — в `.scss`. Для зависимостей ядра применяются абсолютные импорты с префиксом `@/`, обеспечивая модульную изоляцию.

### Интеграция с Cursor
- Фреймворк обеспечивает совместимость с MCP-инфраструктурой Cursor. Реестр серверов и параметры подключения задаются в файле `.cursor/mcp.json` (детальное описание приведено в разделе «Конфигурационные файлы»).

### Правила разработки Cursor (Rules)

Система правил Cursor представляет собой механизм формализации стандартов кодирования, архитектурных паттернов и конвенций проекта, обеспечивающий консистентность разработки при взаимодействии с AI-ассистентом.

#### Назначение системы правил

Правила Cursor решают следующие задачи:

- **Унификация стиля кода**: автоматическое применение соглашений о форматировании, именовании и структуре кода
- **Контекстуализация AI**: предоставление ассистенту специфичной информации об архитектуре проекта, используемых паттернах и ограничениях
- **Предотвращение ошибок**: автоматическое напоминание о критичных требованиях (использование миксинов, запрет на определённые действия)
- **Ускорение разработки**: исключение необходимости повторного объяснения базовых правил проекта при каждом взаимодействии
- **Передача знаний**: документирование неочевидных архитектурных решений и специфичных требований проекта

#### Структура каталога правил

Правила системы располагаются в каталоге `.cursor/rules/` и организованы по доменным областям:

```
.cursor/rules/
├── api_code.mdc          # Правила разработки серверной части (Django)
├── client_code.mdc       # Правила разработки клиентской части (Vue.js)
├── code.mdc              # Общие правила кодирования
├── database.mdc          # Правила работы с базами данных
├── django.mdc            # Специфичные правила Django
├── docs.mdc              # Правила генерации документации
├── mcp.mdc               # Правила использования MCP-серверов
├── oc.mdc                # Правила кроссплатформенной разработки
├── struct.mdc            # Правила архитектурной организации
└── word_docs.mdc         # Правила работы с документами Word
```

#### Формат файлов правил

Правила определяются в файлах формата `.mdc` (Markdown with metadata) со следующей структурой:

```markdown
---
alwaysApply: true
---

# Заголовок раздела

Описание правил и требований в формате Markdown.
```

**Метаданные front matter:**

- `alwaysApply: true` — правило применяется автоматически при каждом взаимодействии с ассистентом без необходимости явного упоминания

#### Классификация правил по доменам

**Архитектурные правила (`struct.mdc`):**
- Описание модульной структуры системы (ядро + модули)
- Принципы изоляции компонентов
- Соглашения об организации файловой структуры

**Правила серверной разработки (`api_code.mdc`, `django.mdc`):**
- Использование обязательных миксинов (например, `SwaggerSafeMixin`)
- Работа со стандартными путями системы
- Запрет на генерацию административной панели Django

**Правила клиентской разработки (`client_code.mdc`):**
- Соглашения об импортах (абсолютные для ядра, относительные для модулей)
- Использование компонентов Bootstrap и библиотеки иконок Lucid
- Декомпозиция компонентов, стилей и логики
- Локализация интерфейса на русском языке

**Правила работы с данными (`database.mdc`):**
- Использование Django ORM и SQLAlchemy
- Создание таблиц посредством миграций
- Паттерны работы со сложными SQL-запросами

**Правила кодирования (`code.mdc`):**
- Ограничение размера файлов (максимум 700 строк)
- Применение принципов ООП и SOLID
- Использование Celery для высоконагруженных алгоритмов
- Адаптация под GPU/CPU

**Правила интеграции (`mcp.mdc`):**
- Использование MCP-серверов для работы с API, базой данных и файловой системой

**Правила кроссплатформенности (`oc.mdc`):**
- Обеспечение совместимости с Windows и Linux

**Правила документирования (`docs.mdc`, `word_docs.mdc`):**
- Запрет на автоматическую генерацию документации
- Использование метрической системы измерений в Word-документах

#### Создание новых правил

Для добавления пользовательского правила:

1. Создайте файл `.mdc` в каталоге `.cursor/rules/`:
   ```bash
   touch .cursor/rules/custom_rule.mdc
   ```

2. Определите структуру правила:
   ```markdown
   ---
   alwaysApply: true
   ---
   
   ### Заголовок правила
   
   Описание требований и примеры использования.
   ```

3. Сохраните файл — правило будет автоматически применяться при следующем взаимодействии с Cursor

**Рекомендации по формулировке правил:**

- Используйте императивную форму для требований («Используй», «Не генерируй», «Всегда применяй»)
- Предоставляйте примеры кода для иллюстрации правил
- Группируйте связанные правила в тематические разделы
- Избегайте избыточной детализации — правила должны быть лаконичными и понятными

#### Применение правил в рабочем процессе

Правила применяются автоматически при каждом обращении к AI-ассистенту благодаря флагу `alwaysApply: true`. Система правил обеспечивает:

- **Контекстную релевантность**: ассистент учитывает специфику проекта при генерации кода
- **Консистентность**: автоматическое следование установленным конвенциям
- **Предотвращение регрессий**: напоминание о критичных ограничениях и требованиях

**Преимущества централизованной системы правил:**

- Отсутствие необходимости повторного объяснения базовых требований
- Синхронизация стандартов кодирования между разработчиками команды
- Документирование архитектурных решений в машиночитаемом формате
- Упрощение онбординга новых участников проекта

### Средства разработки
- Конфигурация интегрированных сред разработки VS Code и Cursor поставляется в составе репозитория. Файлы `.vscode/settings.json`, `.vscode/tasks.json` и `.vscode/extensions.json` определяют воспроизводимую рабочую среду, исключая необходимость ручной настройки (полное описание представлено в разделе «Конфигурационные файлы»).

### Платформенная совместимость
- Система использует унифицированный набор команд развёртывания для операционных систем Windows и Linux. Сценарии, расположенные в `core/deployment/windows` и `core/deployment/linux`, обеспечивают идентичную конфигурацию сервисов, вследствие чего ERGO MS функционирует кроссплатформенно без различий в реализации бизнес-логики и API.

### Управление окружением
- Каталог `virtual_env/` структурирует вспомогательные ресурсы системы: подкаталог `python/` содержит изолированные виртуальные среды Python, `nodejs/` — локальные пакеты Node.js, `celery/` — артефакты очередей сообщений, `trained_models/` — предобученные модели машинного обучения, каталоги `packages/`, `static_api/`, `resources/` — дополнительные данные и статические объекты. Данная стратификация исключает конфликты между модулями и упрощает перенос установки на иное оборудование.

### Модульность библиотек
- Манифест `package.json` определяет npm-рабочие пространства (`core/client`, `modules/*/client`), обеспечивая независимую установку клиентских зависимостей для ядра и каждого модуля в отдельности.
- Конфигурация Poetry (`pyproject.toml`) описывает Python-зависимости: компоненты ядра подключаются посредством локальных пакетов (`core/api`, `core/django`, `core/django_rest_framework`), каталоги `modules/*` экспортируют API-интерфейсы модулей. Дополнительные источники пакетов (`pytorch-cu128`) обеспечивают гибкое управление GPU-зависимостями.

### Командная оболочка ergoms
- Центральный файл конфигурации `core/deployment/commands.conf` определяет агрегированные команды управления: инициализацию режима разработки (`dev`, `client-dev`), развёртывание компонентов (`deploy-api`, `deploy-client`, `deploy-all`), обслуживание зависимостей (`install-deps`, `update-all`, `reinstall`) и управление системными службами Windows/Linux. Контекст выполнения команд задаётся префиксами `poetry:`, `api:`, `npm:`, `shell:`, `win:`, `linux:`.
- Каждый модуль располагает возможностью расширения CLI посредством файла `modules/<имя>/ergoms.conf`. Например, конфигурация `modules/video_analysis/ergoms.conf` добавляет цепочки команд установки моделей машинного обучения (`install`, `install-ffmpeg`, `install-tts-fr`), которые автоматически регистрируются в пространстве имён `ergoms`.
- Единая оболочка обеспечивает кроссплатформенность команд: интерпретатор `ergoms` осуществляет маршрутизацию в PowerShell либо Bash в зависимости от операционной системы, следуя принципам, определённым в `core/deployment/windows` и `core/deployment/linux`.

### Django-автоматизация в рамках ERGOMS

Фреймворк предоставляет набор утилит (`core/api/src/core/utils`), упрощающих работу с Django и Celery:

#### Автоматическое обнаружение приложений

Модуль `core/utils/auto_api/auto_config.py` рекурсивно сканирует директории `core` и `modules`, автоматически регистрирует Django-приложения с файлом `apps.py` в `INSTALLED_APPS` и собирает URL-паттерны из `urls.py`. Это исключает ручное добавление модулей в настройки.

#### Модульная конфигурация Celery

Система `core/utils/celery` позволяет каждому модулю определить собственный `celery_config.py`, наследующий `CeleryModuleConfig`. В нём описываются:
- `get_task_routes()` — маршруты задач модуля в очереди.
- `get_task_queues()` — определение exchange и routing_key.
- `get_task_annotations()` — таймауты, лимиты для конкретных задач.
- `get_module_loggers()` — специализированные логгеры (`celery.module.<имя_модуля>.<логгер>`).

Менеджер `CeleryModuleManager` автоматически обнаруживает конфигурации в `modules/*/celery_config.py`, применяет их в `config/celery.py`, создаёт файлы логов (`logs/modules/<имя>/<имя>.log`) и обеспечивает изолированное логирование задач.

#### Управление переменными окружения

Файл `config/env.py` загружает переменные из корневого `.env`, затем из модульных `.env` (приоритет выше). Утилиты `core/utils/environment/methods.py` сканируют `modules/*/` и `core/*/` на наличие `.env`, объединяя их в единый контекст. Это позволяет модулям переопределять глобальные настройки локально.

#### Работа с базами данных

Модуль `core/utils/database` предоставляет:
- `DBConfig` — парсинг `databases.yaml`, поддержка SSH-туннелей для удалённых БД.
- `SqlAlchemyManager` — выполнение SQL-запросов вне ORM, подключение к множественным источникам (PostgreSQL, MySQL, ClickHouse, MSSQL и др.).
- Структурированные типы соединений (`types.py`) для безопасной работы с конфигурацией.

Рекомендуется создавать `queries.py` в модулях с SQL-шаблонами, передавая их в `SqlAlchemyManager` для выполнения сложных аналитических запросов.

#### Вспомогательные методы и миксины

- `core/utils/methods.py`:
  - `parse_errors_to_dict()` — преобразование ошибок валидации Django в словарь.
  - `send_confirmation_email()` — отправка кодов подтверждения с логированием SMTP-ошибок.
  - `convert_snake_to_camel()`, `convert_path_to_dot_notation()` — утилиты форматирования.

- `core/utils/mixins.py`:
  - `SwaggerSafeMixin` — предотвращает ошибки генерации Swagger при фильтрации queryset по пользователю. Используйте в ViewSet'ах, которые фильтруют данные по `request.user`, чтобы избежать сбоев при построении документации.

### Vue.js-автоматизация в рамках ERGOMS

Клиентская часть фреймворка реализует систему автоматического обнаружения и оркестрации модулей на основе класса `ModuleManager`, обеспечивающего централизованное управление маршрутизацией, навигационным меню, API-эндпоинтами и визуальными компонентами.

#### Архитектура модульной системы

Система построена на принципе иерархической композиции менеджеров:

- **ModuleManager** (`core/client/src/modules/ModuleManager.js`) — центральный оркестратор, координирующий работу специализированных менеджеров:
  - `RouteManager` — управление маршрутами Vue Router
  - `MenuManager` — формирование структуры навигационного меню
  - `EndpointManager` — агрегация API-эндпоинтов
  - `IconManager` — регистрация визуальных компонентов иконок
  - `SeparatorManager` — управление разделителями меню

#### Механизм автоматического обнаружения модулей

При инициализации системы `ModuleManager` рекурсивно сканирует директории `core/client/src` и `modules/*/client/src`, обнаруживая следующие конфигурационные дескрипторы:

1. **`js/routes.js`** — определение маршрутов модуля для Vue Router
2. **`js/menu-config.json`** — конфигурация элементов навигационного меню
3. **`js/endpoints.js`** — экспорт API-эндпоинтов модуля
4. **`components/icons/`** — каталог пользовательских иконок модуля

Обнаруженные конфигурации автоматически агрегируются в единую структуру без необходимости явной регистрации в коде ядра системы.

#### Процесс инициализации

```javascript
// Инициализация менеджера модулей с конфигурацией порядка меню
const moduleManager = new ModuleManager({
  menuOrder: menuOrderConfig
})

// Параллельная инициализация всех менеджеров
await moduleManager.initialize()

// Генерация маршрутов для Vue Router
const routes = await moduleManager.generateAllRoutes(coreRoutes)

// Формирование конфигурации меню
const menuConfig = await moduleManager.getMenuConfig()
```

#### Валидация и диагностика

Система предоставляет встроенные механизмы валидации конфигураций:

```javascript
// Валидация всех конфигураций модулей
const validation = await moduleManager.validateAll()
// Возвращает: { isValid, routes, menu, endpoints }

// Получение статистики по модулям
const stats = await moduleManager.getStatistics()
// Возвращает количество маршрутов, элементов меню, эндпоинтов и т.д.
```

#### Преимущества архитектуры

- **Изоляция модулей**: каждый модуль определяет собственные маршруты, меню и API независимо от ядра
- **Декларативная конфигурация**: использование JSON-дескрипторов вместо императивного кода
- **Автоматическая регистрация**: отсутствие необходимости ручного добавления модулей в центральную конфигурацию
- **Горячая перезагрузка**: поддержка обновления конфигураций в режиме разработки без перезапуска системы
- **Кеширование**: оптимизация производительности посредством интеллектуального кеширования обнаруженных конфигураций

### Интеграция Bootstrap и стилевая архитектура

Система клиентского приложения использует архитектуру SCSS-модулей с интегрированным фреймворком Bootstrap 5, обеспечивающую консистентное визуальное оформление и адаптивную вёрстку.

#### Структура стилевых модулей

Централизованная стилевая система располагается в каталоге `core/client/src/scss/` и организована следующим образом:

- **`_variables.scss`** — глобальные переменные дизайн-системы (размеры, отступы, радиусы, анимации)
- **`_colors.scss`** — палитра цветов и тематические переменные
- **`_theme.scss`** — определение светлой и тёмной тем оформления
- **`_mixins.scss`** — переиспользуемые SCSS-миксины для типовых паттернов
- **`_globals.scss`** — глобальные стили HTML-элементов и утилитарных классов
- **`_overrides.scss`** — переопределение стандартных стилей Bootstrap
- **`_scrollbar.scss`** — кастомизация полос прокрутки
- **`styles.scss`** — главная точка входа, импортирующая все модули

#### Интеграция Bootstrap

Bootstrap импортируется после определения пользовательских переменных, что позволяет переопределить стандартные параметры фреймворка:

```scss
@import './_colors.scss';
@import './_theme.scss';
@import './_variables.scss';
@import './_overrides.scss';

@import 'bootstrap/scss/bootstrap';
```

Такая последовательность обеспечивает:

1. Применение кастомных переменных к компонентам Bootstrap
2. Переопределение стандартных стилей без модификации исходного кода фреймворка
3. Сохранение совместимости с обновлениями Bootstrap

#### Преимущества использования Bootstrap

- **Адаптивная сетка**: встроенная система сеток обеспечивает корректное отображение на устройствах различных размеров
- **Унифицированные компоненты**: стандартизированные UI-элементы (кнопки, формы, модальные окна) с консистентным поведением
- **Утилитарные классы**: обширная библиотека классов для быстрого прототипирования интерфейсов
- **Тематизация**: возможность глобального изменения визуального стиля посредством модификации SCSS-переменных
- **Доступность**: соответствие стандартам WCAG для создания инклюзивных интерфейсов

#### Модульная архитектура стилей

Каждый модуль системы располагает возможностью определения собственных стилей в файлах `*.scss`, которые автоматически интегрируются в общую систему при сборке проекта. Это обеспечивает инкапсуляцию стилей и предотвращает конфликты именования.

### Агрегация переиспользуемых компонентов

Каталог `core/client/src/components/` содержит библиотеку универсальных Vue.js-компонентов, предназначенных для упрощения разработки пользовательских интерфейсов модулей системы.

#### Классификация компонентов

**Диалоговые компоненты:**

- **`ConfirmDialog.vue`** — модальное окно подтверждения операций с настраиваемыми заголовком, сообщением и кнопками действий
- **`ChoiceDialog.vue`** — диалог выбора из множества опций с поддержкой радиокнопок и чекбоксов
- **`ModalCenter.vue`** — базовый компонент центрированного модального окна с управлением жизненным циклом

**Компоненты форм и ввода данных:**

- **`SelectBox.vue`** — кастомный выпадающий список с поддержкой поиска, множественного выбора и виртуализации
- **`DropDown.vue`** — универсальный выпадающий список с настраиваемым контентом
- **`TableEditor.vue`** — табличный редактор данных с inline-редактированием и валидацией

**Навигационные компоненты:**

- **`NavigationButtons.vue`** — группа кнопок навигации с поддержкой состояний (активная, неактивная, disabled)
- **`menu/`** — набор компонентов для построения многоуровневого навигационного меню
- **`header/`** — компоненты шапки приложения (профиль пользователя, настройки, уведомления)

**Уведомления:**

- **`NotificationProvider.vue`** — провайдер системы уведомлений с управлением очередью сообщений
- **`NotificationToast.vue`** — визуальный компонент всплывающего уведомления

**Прочие утилитарные компоненты:**

- **`DefaultAvatar.vue`** — генератор аватаров по умолчанию на основе инициалов пользователя
- **`NotFound.vue`** — страница ошибки 404

#### Принципы разработки компонентов

1. **Переиспользуемость**: компоненты проектируются для использования в различных контекстах без модификации
2. **Параметризация**: расширенная система входных параметров (props) для настройки поведения и внешнего вида
3. **Событийная модель**: использование событий Vue.js для коммуникации с родительскими компонентами
4. **Слоты**: поддержка слотов для инжекции пользовательского контента
5. **Композиционная архитектура**: декомпозиция сложных компонентов на более простые составляющие
6. **Реактивность**: использование Composition API для оптимального управления реактивным состоянием

#### Интеграция в модули

Компоненты доступны модулям посредством абсолютных импортов с префиксом `@/components/`:

```javascript
import ConfirmDialog from '@/components/ConfirmDialog.vue'
import SelectBox from '@/components/SelectBox.vue'
import NotificationProvider from '@/components/NotificationProvider.vue'
```

Такая архитектура исключает дублирование кода между модулями и обеспечивает консистентный пользовательский опыт во всех компонентах системы.

## Структура проекта

### Конфигурационные файлы
- `.env.example` / `.env` — базовая спецификация переменных окружения. Глобальный `.env` определяет поведение ядра, тогда как модульные `.env` в `modules/*` и `core/*` расширяют конфигурацию локальными параметрами (путь к пакетам, GPU-настройки, ключи интеграций).
- `databases.yaml.example` / `databases.yaml` — декларативное описание подключений к основным и вспомогательным БД. Поддерживаются PostgreSQL, MySQL и SQLite; строки со специальными символами заключаются в двойные кавычки.
- `menu-order-config.json.example` / `menu-order-config.json` — схема сортировки пунктов меню клиентского приложения. При отсутствии пользовательского файла задействуется дефолтный конфиг ядра.
- `.cursor/mcp.json` — реестр MCP-серверов, автоматически доступных из Cursor (`ergo-api`, `ergo-filesystem`, `ergo-database-postgres` и др.). Позволяет выполнять запросы к API, файловой системе и БД без ручной настройки.
- `.cursor/rules/*.mdc` — правила разработки для AI-ассистента Cursor, определяющие стандарты кодирования, архитектурные паттерны и конвенции проекта. Применяются автоматически при каждом взаимодействии с ассистентом.
- `.cursorignore` — фильтр индексации Cursor; исключает крупные бинарные артефакты (изображения, базы данных и т.д.) для ускорения поиска.
- `.vscode/settings.json` — единые правила форматирования, линтинга и отображения для VS Code/Cursor.
- `.vscode/tasks.json` — сценарии автоматизации для установки зависимостей, запуска сервисов и работы с модулями на обеих платформах.
- `.vscode/extensions.json` — рекомендуемый набор расширений для синхронизации команды.
- `core/deployment/commands.conf` — описание агрегированных команд CLI, которые используются задачами `tasks.json` и обёрткой `ergoms`.
- `modules/*/ergoms.conf` — дополнительные команды для конкретных модулей, подключаемые к общему CLI-пространству.
- `modules/*/client/js/menu-config.json` — локальные схемы меню модулей, сочетание которых формирует итоговую клиентскую навигацию.
- `pyrightconfig.json` — границы статического анализа Python и дополнительные пути импорта для форков Django/DRF.
- Прочие справочные файлы (`README.md`, `logic.md` в подпапках) документируют архитектурные решения и не содержат исполняемой логики.

## Быстрый старт

> **⚠️ КРИТИЧНОЕ ТРЕБОВАНИЕ:** Абсолютный путь к корневому каталогу системы должен содержать исключительно символы латинского алфавита (A-Z, a-z), цифры (0-9), дефис (-) и нижнее подчёркивание (_). Использование символов кириллицы, иероглифов или специальных символов в пути файловой системы приведёт к критическим ошибкам инициализации модулей и сбоям в работе инструментов сборки (Vite, Poetry, npm).
> 
> **Корректные примеры путей:**
> - `C:\projects\ergo_ms\`
> - `/var/www/ergo-ms/`
> - `/home/user/projects/ergo_ms_core/`
> 
> **Некорректные примеры путей:**
> - `C:\проекты\ergo_ms\` ❌
> - `/home/пользователь/ergo_ms/` ❌
> - `C:\projects\ergo ms\` ❌ (пробел в пути)

**Этап 1. Требуемые программные зависимости**

Система предполагает наличие следующих компонентов инфраструктуры:
- Python 3.12 ([официальный сайт](https://www.python.org/downloads/))
- Node.js 18+ ([официальный сайт](https://nodejs.org/))
- PostgreSQL 14+ ([официальный сайт](https://www.postgresql.org/download/))
- Git ([официальный сайт](https://git-scm.com/downloads))

**Этап 2. Клонирование репозитория**

```cmd
git clone <repository-url> ergo_ms_core
cd ergo_ms_core
```

**Этап 3. Инициализация конфигурационных файлов**

```cmd
copy .env.example .env
copy databases.yaml.example databases.yaml
copy menu-order-config.json.example menu-order-config.json
```

Полученные конфигурационные дескрипторы `.env` и `databases.yaml` подлежат корректировке в соответствии с параметрами целевой инфраструктуры (детальная спецификация представлена в разделе 3.2).

**Этап 4. Установка зависимостей и инициализация окружения**

Процедура выполняется одним из следующих способов:

*Вариант A: интегрированная среда разработки (VS Code/Cursor)*
```
Ctrl+Shift+P → Tasks: Run Task → Install ErgoMS CLI
Ctrl+Shift+P → Tasks: Run Task → Setup Full System
```

*Вариант B: командная строка PowerShell (требует административных привилегий)*
```powershell
cd C:\path\to\ergo_ms_core
.\core\deployment\windows\ergo_ms.ps1 install-cli
ergoms setup-full
```

> **⚠️ Требование безопасности:** процедура установки CLI и регистрации системных служб требует административных привилегий операционной системы.

**Этап 5. Запуск системы в режиме разработки**

*Рекомендуемый способ (VS Code/Cursor):*
```
Ctrl+Shift+B
```

*Альтернативный способ (CLI):*
```cmd
ergoms start
```

> **ℹ️ Особенность архитектуры:** в режиме разработки Django инициализирует два параллельных процесса: основной обрабатывает HTTP-запросы, вспомогательный осуществляет мониторинг изменений исходного кода и выполняет горячую перезагрузку модулей. Данная конфигурация соответствует штатному режиму функционирования.

**Этап 6. Сетевые точки доступа**

- Клиентское веб-приложение: `http://localhost:8001`
- API-сервер: `http://localhost:8000`

## Настройка конфигурационных файлов

### Настройка .env

**Процедура конфигурации:**

1. Создайте файл `.env` на основе шаблона `.env.example`
2. Определите следующие параметры окружения:
   ```env
   # Общие настройки системы
   SYSTEM_TITLE=ERGO MS      # Название системы
   
   # Настройки API
   API_HOST=localhost        # Хост для API сервера
   API_PORT=8000            # Порт API сервера
   API_ALLOWED_HOSTS=localhost,127.0.0.1  # Разрешенные хосты
   API_DEPLOY_TYPE=development  # Тип развертывания (development/production)
   
   # Настройки токенов
   API_ACCESS_TOKEN_LIFETIME=30    # Время жизни access токена (в минутах)
   API_REFRESH_TOKEN_LIFETIME=1440 # Время жизни refresh токена (в минутах, 1440 = 1 день)
   
   # Ограничения запросов
   API_THROTTLE_RATES_ANON=10/minute  # Лимит запросов для анонимных пользователей
   API_THROTTLE_RATES_USER=5000/hour  # Лимит запросов для авторизованных пользователей
   
   # Безопасность
   API_SECRET_KEY=secret-key  # Секретный ключ (замените на сложный случайный ключ)
   
   # Настройки клиента
   CLIENT_HOST=localhost      # Хост клиентского приложения
   CLIENT_PORT=8001          # Порт клиентского приложения
   
   # Настройки почты
   EMAIL_HOST_USER=mail           # Адрес электронной почты, с которой будут высылаться коды
   EMAIL_HOST_PASSWORD=password   # Почтовый пароль для внешних приложений (НУЖЕН ПАРОЛЬ НЕ ОТ УЧЕТНОЙ ЗАПИСИ)
   EMAIL_HOST=smtp.mail.ru        # SMTP сервер
   EMAIL_PORT=465                 # Порт SMTP сервера
   EMAIL_USE_TLS=False
   EMAIL_USE_SSL=True

   VITE_YANDEX_MAPS_API_KEY=secret-key      
   ```

**Критичные параметры безопасности и производительности:**

- `API_SECRET_KEY`: в производственной среде требуется криптографически стойкий случайный ключ
- `API_DEPLOY_TYPE`: в производственной конфигурации устанавливается значение `production`
- `API_ALLOWED_HOSTS`: перечень доменных имён, авторизованных для обращения к API
- `API_THROTTLE_RATES`: лимиты частоты запросов определяются в соответствии с требованиями к пропускной способности
- Email-конфигурация: рекомендуется использование выделенной учётной записи электронной почты

### Настройка databases.yaml

**Процедура конфигурации:**

1. Создайте файл `databases.yaml` на основе шаблона `databases.yaml.example`
2. Спецификация структуры конфигурационного дескриптора:
   ```yaml
   databases:
    default:
      engine: "postgresql"
      name: "ergo_ms"
      user: "postgres"
      password: "admin"
      host: "localhost"
      port: 5432

    celery:
      engine: "postgresql"
      name: "ergo_ms_celery"
      user: "celery_user"
      password: "celery_password"
      host: "localhost"
      port: 5432
  
    celery_worker:
      engine: "postgresql"
      name: "ergo_ms_celery_worker"
      user: "celery_worker_user"
      password: "worker_password"
      host: "localhost"
      port: 5432
  
    celery_beat:
      engine: "postgresql"
      name: "ergo_ms_celery_beat"
      user: "celery_beat_user"
      password: "beat_password"
      host: "localhost"
      port: 5432

    analytics:
      engine: "mysql"
      name: "world"
      user: "root"
      password: "admin"
      host: "localhost"
      port: 3306
   ```

#### Руководство по форматированию YAML

**Правила экранирования специальных символов:**

При редактировании YAML-дескрипторов строковые литералы, содержащие метасимволы синтаксиса YAML, подлежат обязательному заключению в двойные кавычки:

```
! : > | & * # { } [ ] ,
```

**Корректные примеры:**

```yaml
password: "!admin123"
hostname: "server.example.com"
query: "SELECT * FROM users"
path: "/home/user/data"
command: "grep -v '#' file.txt"
```

**Некорректная запись:**

```yaml
password: !admin123      # Ошибка: символ '!' интерпретируется парсером как YAML-тег
```

**Обоснование требования:**

YAML-спецификация определяет набор метасимволов синтаксиса. При отсутствии экранирования парсер ошибочно интерпретирует данные символы как элементы структуры документа, что приводит к нарушению семантической целостности конфигурации.

**Рекомендации по форматированию:**

- Строковые литералы, содержащие метасимволы, безусловно заключаются в двойные кавычки
- При наличии неопределённости относительно требования экранирования следует применять кавычки
- Пароли и прочие параметры безопасности требуют особого внимания к экранированию
- После модификации YAML-файлов рекомендуется верификация с использованием специализированного валидатора

### Настройка menu-order-config.json

Дескриптор `menu-order-config.json` определяет порядок отображения элементов навигационного меню клиентского приложения. Данный файл является опциональным; при его отсутствии система применяет конфигурацию по умолчанию, расположенную в `core/client/src/config/menu-order-config.json`.

**Процедура конфигурации:**

1. Создайте файл на основе шаблона:
   ```cmd
   copy menu-order-config.json.example menu-order-config.json
   ```

2. Спецификация структуры дескриптора:
   ```json
   {
     "menuOrder": [
       "User",
       "Settings",
       "BI"
     ],
     
     "separators": {
       "0": "Настройки",
       "2": "Модули"
     }
   }
   ```

3. Семантика параметров конфигурации:
   - `menuOrder`: упорядоченный массив идентификаторов маршрутов
   - `separators`: ассоциативный массив индексов элементов меню с соответствующими текстовыми метками разделителей

**Критичные требования:**

- Файл размещается в корневом каталоге проекта (на уровне `README.md`)
- После модификации файла требуется перезапуск сервера разработки
- Система осуществляет автоматический выбор конфигурационного дескриптора при инициализации
- При отсутствии пользовательского файла применяется конфигурация по умолчанию без генерации ошибок
- Идентификаторы маршрутов должны строго соответствовать определениям в модулях системы
- Индексация разделителей осуществляется с нулевого элемента

**Примеры конфигурации:**

*Переопределение порядка элементов меню:*
```json
{
  "menuOrder": [
    "BI",
    "User",
    "Settings"
  ]
}
```

*Добавление разделителей:*
```json
{
  "menuOrder": [
    "User",
    "Settings",
    "BI",
    "Reports"
  ],
  "separators": {
    "0": "Пользователь",
    "2": "Аналитика"
  }
}
```

**Верификация применения конфигурации:**

При инициализации сервера разработки система выводит диагностическое сообщение:
- `✓ Используется пользовательская конфигурация меню из корня системы` — обнаружен пользовательский дескриптор
- `✓ Используется стандартная конфигурация меню` — применяется конфигурация по умолчанию

### Возможность использования совместной работы на одном компьютере

Система поддерживает режим коллаборативной разработки посредством механизма совместного доступа к рабочей директории. Инициатор сеанса транслирует файловую систему проекта удалённым участникам, обеспечивая синхронное редактирование исходного кода.

**Технология реализации:** расширение [Live Share](https://visualstudio.microsoft.com/ru/services/live-share/) для Visual Studio Code/Cursor

**Процедура инициализации сеанса:**

1. Установите расширение из [официального репозитория](https://marketplace.visualstudio.com/items?itemName=MS-vsliveshare.vsliveshare)
2. Откройте проект в интегрированной среде разработки
3. Активируйте функцию «Live Share» посредством соответствующей кнопки интерфейса
4. Распространите сгенерированную ссылку участникам сеанса

Конфигурационный дескриптор `.vsls.json` автоматически применяется к сеансу, обеспечивая корректную видимость вложенных репозиториев и определяя область доступа участников.

**Детальная документация:** [официальное руководство Microsoft](https://learn.microsoft.com/en-us/visualstudio/liveshare/quickstart/share)

**Требование сетевой конфигурации:** для обеспечения полного функционала системы участникам сеанса требуется доступ к TCP-портам 8000 и 8001 (либо портам, определённым в `.env`). Порт 8001 обслуживает клиентское веб-приложение, порт 8000 — API-сервер.

## Запуск системы разработки

**Архитектурные компоненты системы:**

- **CLIENT** — клиентское веб-приложение на базе Vue.js (TCP-порт 8001)
- **API** — серверное приложение на базе Django (TCP-порт 8000)  
- **CELERY WORKER** — обработчик асинхронных фоновых задач
- **CELERY BEAT** — планировщик периодических задач

### Хранение логов

Система осуществляет централизованное логирование в каталоге `logs/`. Структура каталога включает:

- Журналы подсистемы Celery: `celery.log`, `celery_worker.log`, `celery_beat.log`
- Журналы модулей системы: `video_analysis.log`, `porosity_tasks.log` и их ротационные копии
- Диагностические отчёты и трассировки исключений
- Структурированные подкаталоги: `modules/`, `celery/`, `screenshots/`

Централизация журналов в единой точке файловой системы обеспечивает упрощённую диагностику сбоев, анализ производительности и аудит безопасности.

### Управление сервисами через CLI

Командная оболочка `ergoms` осуществляет агрегацию команд из центрального дескриптора `core/deployment/commands.conf` и модульных конфигураций `modules/*/ergoms.conf`. Классификация команд представлена ниже.

#### Базовые операции

Управление жизненным циклом всех сервисов системы (API, Client, Celery Worker, Celery Beat):

```cmd
ergoms start      # Инициализация всех сервисов
ergoms stop       # Завершение работы всех сервисов
ergoms restart    # Перезапуск всех сервисов
ergoms status     # Получение статуса всех сервисов
```

#### Компонентный запуск

Избирательная инициализация отдельных компонентов системы:

```cmd
ergoms dev              # API в режиме разработки (api:dev)
ergoms start-client     # Клиентское приложение (npm:run dev)
ergoms client-dev       # Эквивалент start-client
ergoms start-worker     # Обработчик фоновых задач (api:start_celery_worker)
ergoms start-beat       # Планировщик периодических задач (api:start_celery_beat)
```

#### Наблюдение за системой

Инспекция журналов сервисов с параметрами глубины вывода:

```cmd
ergoms logs ergo-api-dev           # Последние 500 записей журнала API
ergoms logs ergo-client-dev 1000   # Последние 1000 записей журнала Client
ergoms logs ergo-celery-worker     # Журнал обработчика фоновых задач
ergoms logs ergo-celery-beat       # Журнал планировщика задач
```

#### Миграции и статика

Операции обслуживания схемы базы данных и статических ресурсов:

```cmd
ergoms db-migrate           # Применение миграций к базе данных
ergoms db-makemigrations    # Генерация новых миграций
ergoms migrate-all          # Генерация и применение миграций (единая транзакция)
ergoms collectstatic        # Агрегация статических файлов
```

#### Управление зависимостями

Операции с пакетными зависимостями Python и Node.js:

```cmd
ergoms setup                # Полная инициализация: poetry:install && npm:install && api:migrate
ergoms python-install       # Установка Python-зависимостей (poetry:install)
ergoms python-update        # Обновление Python-зависимостей (poetry:update)
ergoms reinstall            # Переустановка с синхронизацией: poetry:install --sync && npm:ci
ergoms update-all           # Обновление всех зависимостей: poetry:update && npm:update
```

#### Сборка проекта

Подготовка артефактов для производственного развёртывания:

```cmd
ergoms build-all            # Полная сборка: npm:run build && api:collectstatic --noinput
ergoms client-build         # Сборка клиентского приложения (npm:run build)
ergoms collectstatic        # Сборка статических ресурсов (api:collectstatic --noinput)
```

### Дополнительные команды CLI

#### Установка и управление службами

Регистрация системных служб в операционной системе (требуется повышение привилегий):

```cmd
ergoms install              # Регистрация и запуск служб Windows
ergoms uninstall            # Удаление служб (опция -Purge удаляет пользовательские данные)
ergoms install-all-services # Регистрация всех служб Windows
ergoms install-all-services-linux # Регистрация всех служб Linux (systemd)
```

**Платформенные скрипты прямого вызова:**

- `core/deployment/windows/ergo_ms.ps1` — скрипт PowerShell для Windows
- `core/deployment/linux/ergo_ms.sh` — скрипт Bash для Linux

Скрипты реализуют операции: `setup-full`, `clean-project`, управление жизненным циклом служб.

#### Управление CLI-обёрткой

```cmd
ergoms install-cli    # Установка CLI-обёртки (требуется повышение привилегий)
ergoms uninstall-cli  # Удаление CLI-обёртки (требуется повышение привилегий)
ergoms help           # Вывод справочной информации по доступным командам
ergoms setup-full     # Полная инициализация системы (git, venv, poetry, npm, services; требуется повышение привилегий)
```

#### Комплексные сценарии развёртывания

Автоматизированное развёртывание с обновлением субмодулей, установкой зависимостей и сборкой:

```cmd
ergoms deploy-api         # API-развёртывание: обновление субмодулей, установка зависимостей, применение миграций
ergoms deploy-client      # Client-развёртывание: обновление субмодуля, установка зависимостей, сборка проекта
ergoms deploy-all         # Полное развёртывание: обновление всех субмодулей ядра, установка зависимостей, сборка
ergoms deploy-api-dev     # Развёртывание API с запуском в режиме разработки
ergoms deploy-client-dev  # Развёртывание Client с запуском в режиме разработки
```

**Семантика префиксов команд:**

Префиксы `shell:`, `poetry:`, `npm:`, `win:`, `linux:` определяют контекст исполнения. Система автоматически выбирает интерпретатор команд (PowerShell или Bash) на основе детектирования операционной системы.

### Прокси-команды

Прямая переадресация к инструментальным утилитам (не требуется повышение привилегий):

```cmd
ergoms poetry <args>     # Переадресация к Poetry
ergoms api <args>        # Переадресация к Django manage.py
ergoms npm <args>        # Переадресация к npm
```

**Примеры использования:**

```cmd
# Операции Poetry
ergoms poetry install
ergoms poetry update
ergoms poetry add <package>

# Команды Django API
ergoms api migrate
ergoms api createsuperuser
ergoms api shell

# Операции npm
ergoms npm run dev
ergoms npm install
```

### Модульные команды

Каждый модуль расширяет CLI посредством собственного конфигурационного дескриптора `ergoms.conf`. Иллюстрация на примере модуля `video_analysis`:

- `ergoms video_analysis:install` — установка всех зависимостей модуля (`install_ffmpeg`, `install_opus_mt`, `install_vosk_model`, `install_silero_repo`, `install_tts_model`)
- `ergoms video_analysis:install-ffmpeg`, `install-opus`, `install-vosk`, `install-silero`, `install-tts-fr` — избирательная установка компонентов

При отсутствии коллизий имён команды доступны без префикса модуля. Актуальный реестр команд: `ergoms help`.

**Синтаксис вызова:**
```cmd
ergoms <module>:<command>       # Квалифицированный вызов с префиксом модуля
ergoms <command>                # Неквалифицированный вызов (при отсутствии коллизий)
```

### Практические сценарии

**Полная инициализация системы:**

```cmd
# Windows (PowerShell с повышением привилегий)
.\core\deployment\windows\ergo_ms.ps1 install-cli
ergoms setup-full

# Linux (Bash с привилегиями суперпользователя)
sudo bash core/deployment/linux/ergo_ms.sh install-cli
sudo ergoms setup-full
```

**Цикл ежедневной разработки:**

```cmd
ergoms start                # Инициализация всех сервисов
ergoms logs ergo-api-dev    # Инспекция журналов
ergoms stop                 # Завершение работы сервисов
```

**Операции с базой данных:**

```cmd
ergoms migrate-all          # Генерация и применение миграций
ergoms api createsuperuser  # Создание учётной записи суперпользователя
ergoms api shell            # Интерактивная оболочка Django
```

**Управление зависимостями:**

```cmd
ergoms setup                # Полная установка зависимостей (poetry + npm + migrate)
ergoms poetry add <package> # Добавление Python-пакета
ergoms npm install <package># Добавление npm-пакета
```

**Подготовка к производственному развёртыванию:**

```cmd
ergoms build-all            # Сборка клиента и агрегация статических ресурсов
```

### Конфигурация CLI

**Структура конфигурационных дескрипторов:**

- Команды ядра системы: `core/deployment/commands.conf`
- Команды модулей: `modules/*/ergoms.conf`

**Синтаксис определения команд:**

```conf
# Комментарий
command-name=type:command

# Типология префиксов:
# poetry: - команда Poetry (контекст выполнения: core/)
# api:    - команда Django manage.py
# npm:    - команда npm
# shell:  - команда оболочки (кроссплатформенная)
# win:    - команда Windows (игнорируется на Linux)
# linux:  - команда Linux (игнорируется на Windows)
```

**Пример конфигурации ядра (core/deployment/commands.conf):**

```conf
# Команды API
dev=api:dev
db-migrate=api:migrate
migrate-all=api:makemigrations && api:migrate

# Команды Client
start-client=npm:run dev
client-build=npm:run build

# Команды Celery
start-worker=api:start_celery_worker
start-beat=api:start_celery_beat

# Управление зависимостями
setup=poetry:install && npm:install && api:migrate
python-install=poetry:install
update-all=poetry:update && npm:update

# Сборка артефактов
build-all=npm:run build && api:collectstatic --noinput
```

**Пример конфигурации модуля (modules/video_analysis/ergoms.conf):**

```conf
# Установка всех зависимостей модуля
install=api:install_ffmpeg && api:install_opus_mt && api:install_vosk_model
install-ffmpeg=api:install_ffmpeg
install-opus=api:install_opus_mt
```

**Требования к привилегиям:**

- Повышение привилегий требуется для: `install`, `start`, `stop`, `restart`, `status`, `uninstall`, `install-cli`, `setup-full`
- Прокси-команды и пользовательские команды не требуют повышения привилегий

**Завершение работы системы:**

```cmd
ergoms stop
```

Альтернативный способ: завершение процессов терминалов посредством `Ctrl+C` в каждом активном сеансе.

## Решение проблем при установке

### Проблема 1: Permission denied при установке Poetry пакетов

**Диагностическая сигнатура:**

```
[Errno 13] Permission denied: Poetry Cache
```

**Процедура устранения:**

```cmd
# Очистка кеша Poetry
rmdir /S /Q "%LOCALAPPDATA%\pypoetry\Cache"

# Переустановка зависимостей без использования кеша
cd core
poetry install --no-cache
```

### Проблема 2: Команда ergoms не найдена

**Процедура устранения:**

```cmd
# PowerShell с повышением привилегий
.\core\deployment\windows\ergo_ms.ps1 install-cli
```

### Проблема 3: База данных не настроена

**Процедура устранения:**

1. Создайте конфигурационный дескриптор `databases.yaml` на основе шаблона `databases.yaml.example`
2. Определите параметры подключения к СУБД в соответствии с инфраструктурой
3. Примените миграции: `ergoms db-migrate`

## Разворачивание системных служб (Linux) — скрипт `linux/ergo_ms.sh`

Скрипт осуществляет регистрацию и управление службами systemd без использования жёстко заданных путей. Конфигурация пути к корневому каталогу системы определяется посредством файла окружения `/etc/default/ergo_ms` (переменная `ERGO_ROOT`).

**Поддерживаемые операции:**

```bash
sudo bash /var/www/ergo_ms/linux/ergo_ms.sh install --root /var/www/ergo_ms   # Регистрация служб и инициализация
sudo bash /var/www/ergo_ms/linux/ergo_ms.sh start                             # Запуск всех служб
sudo bash /var/www/ergo_ms/linux/ergo_ms.sh stop                              # Остановка всех служб
sudo bash /var/www/ergo_ms/linux/ergo_ms.sh restart                           # Перезапуск всех служб
sudo bash /var/www/ergo_ms/linux/ergo_ms.sh status                            # Получение статуса всех служб
sudo bash /var/www/ergo_ms/linux/ergo_ms.sh uninstall [--purge]               # Удаление служб (опция --purge удаляет /etc/default/ergo_ms)
```

**CLI-обёртка `ergoms`:**

```bash
# Установка CLI-обёртки
sudo bash /var/www/ergo_ms/linux/ergo_ms.sh install-cli

# Использование CLI
sudo ergoms install --root /var/www/ergo_ms
sudo ergoms start | stop | restart | status
sudo ergoms uninstall [--purge]

# Удаление CLI-обёртки
sudo ergoms uninstall-cli
```

**Идентификаторы служб systemd:**

- `ergo-api-dev.service` — серверное приложение Django в режиме разработки
- `ergo-client-dev.service` — клиентское приложение Vue.js в режиме разработки
- `ergo-celery-worker.service` — обработчик асинхронных задач Celery
- `ergo-celery-beat.service` — планировщик периодических задач Celery

**Инспекция журналов systemd:**

```bash
journalctl -u ergo-api-dev -n 500 -f
journalctl -u ergo-client-dev -n 500 -f
journalctl -u ergo-celery-worker -n 500 -f
journalctl -u ergo-celery-beat -n 500 -f
```